@model WarehouseModel

@{
    //page title
    ViewBag.Title = T("Merchant.Configuration.Shipping.Warehouses.AddNew").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Warehouses");
}

<form asp-controller="Shipping" asp-action="CreateWarehouse" method="post">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Merchant.Configuration.Shipping.Warehouses.AddNew")
            <small>
                <i class="fa fa-arrow-circle-left"></i>
                <a asp-action="Warehouses">@T("Merchant.Configuration.Shipping.Warehouses.BackToList")</a>
            </small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Merchant.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Merchant.Common.SaveContinue")
            </button>
            @await Component.InvokeAsync("MerchantWidget", new { widgetZone = MerchantWidgetZones.WarehouseDetailsButtons })
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdateWarehouse", Model)
</form>