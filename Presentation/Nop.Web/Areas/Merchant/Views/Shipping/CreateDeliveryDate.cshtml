@model DeliveryDateModel

@{
    //page title
    ViewBag.Title = T("Merchant.Configuration.Shipping.DeliveryDates.AddNew").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Delivery dates");
}

<form asp-controller="Shipping" asp-action="CreateDeliveryDate" method="post">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Merchant.Configuration.Shipping.DeliveryDates.AddNew")
            <small>
                <i class="fa fa-arrow-circle-left"></i>
                <a asp-action="DatesAndRanges">@T("Merchant.Configuration.Shipping.DeliveryDates.BackToList")</a>
            </small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Merchant.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Merchant.Common.SaveContinue")
            </button>
            @await Component.InvokeAsync("MerchantWidget", new { widgetZone = MerchantWidgetZones.DeliveryDateDetailsButtons })
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdateDeliveryDate", Model)
</form>