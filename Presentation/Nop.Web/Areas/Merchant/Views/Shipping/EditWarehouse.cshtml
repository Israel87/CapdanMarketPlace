@model WarehouseModel

@{
    //page title
    ViewBag.Title = T("Merchant.Configuration.Shipping.Warehouses.EditWarehouseDetails").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Warehouses");
}

<form asp-controller="Shipping" asp-action="EditWarehouse" method="post">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Merchant.Configuration.Shipping.Warehouses.EditWarehouseDetails") - @Model.Name
            <small>
                <i class="fa fa-arrow-circle-left"></i>
                <a asp-action="Warehouses">@T("Merchant.Configuration.Shipping.Warehouses.BackToList")</a>
            </small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Merchant.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Merchant.Common.SaveContinue")
            </button>
            <span id="warehouse-delete" class="btn bg-red">
                <i class="fa fa-trash-o"></i>
                @T("Merchant.Common.Delete")
            </span>
            @await Component.InvokeAsync("MerchantWidget", new { widgetZone = MerchantWidgetZones.WarehouseDetailsButtons, additionalData = Model })
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdateWarehouse", Model)
</form>
<nop-delete-confirmation asp-model-id="@Model.Id" asp-button-id="warehouse-delete" asp-action="DeleteWarehouse" />
