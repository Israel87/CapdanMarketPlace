@model VendorAttributeModel

@{
    //page title
    ViewBag.Title = T("Merchant.Vendors.VendorAttributes.AddNew").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Vendor and user settings");
}

<form asp-controller="VendorAttribute" asp-action="Create" method="post" id="vendorattribute-form">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Merchant.Vendors.VendorAttributes.AddNew") 
            <small>
                <i class="fa fa-arrow-circle-left"></i>
                <a asp-action="List">@T("Merchant.Vendors.VendorAttributes.BackToList")</a>
            </small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Merchant.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Merchant.Common.SaveContinue")
            </button>
            @await Component.InvokeAsync("MerchantWidget", new { widgetZone = "Merchant_vendor_attribute_details_buttons" })
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdate", Model)
</form>