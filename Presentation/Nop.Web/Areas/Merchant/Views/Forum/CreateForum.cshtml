@model ForumModel

@{
    //page title
    ViewBag.Title = T("Merchant.ContentManagement.Forums.Forum.CreateForum").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Manage forums");
}

<form asp-controller="Forum" asp-action="CreateForum" method="post">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Merchant.ContentManagement.Forums.Forum.CreateForum") 
            <small>
                <i class="fa fa-arrow-circle-left"></i>
                <a asp-action="List">@T("Merchant.ContentManagement.Forums.Forum.BackToList")</a>
            </small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Merchant.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Merchant.Common.SaveContinue")
            </button>
            @await Component.InvokeAsync("MerchantWidget", new { widgetZone = MerchantWidgetZones.ForumDetailsButtons })
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdateForum", Model)
</form>