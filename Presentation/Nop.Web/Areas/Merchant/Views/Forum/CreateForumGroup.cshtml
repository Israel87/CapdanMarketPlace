@model ForumGroupModel

@{
    //page title
    ViewBag.Title = T("Merchant.ContentManagement.Forums.ForumGroup.CreateForumGroup").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Manage forums");
}

<form asp-controller="Forum" asp-action="CreateForumGroup" method="post">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Merchant.ContentManagement.Forums.ForumGroup.CreateForumGroup")
            <small>
                <i class="fa fa-arrow-circle-left"></i>
                <a asp-action="List">@T("Merchant.ContentManagement.Forums.ForumGroup.BackToList")</a>
            </small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Merchant.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Merchant.Common.SaveContinue")
            </button>
            @await Component.InvokeAsync("MerchantWidget", new { widgetZone = MerchantWidgetZones.ForumGroupDetailsButtons })
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdateForumGroup", Model)
</form>